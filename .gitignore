# editor
*~
*.swp
*.tmp

# zsh
.zcompdump*

# dependencies (opencode/ has its own scoped ignores for its local-only files)
node_modules/

# tmux plugin clones (managed by tpm / install.sh)
tmux/plugins/
