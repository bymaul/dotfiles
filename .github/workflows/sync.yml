name: sync

on:
  push:
    branches: [master]
    paths:
      - "nvim/**"
      - "starship/**"
      - "bat/**"
      - "lazygit/**"
      - "opencode/**"
      - "zsh/**"
      - "tmux/**"
      - "fastfetch/**"
  workflow_dispatch:

concurrency:
  group: sync-shared
  cancel-in-progress: true

jobs:
  sync:
    runs-on: ubuntu-latest
    env:
      OTHER_REPO: bymaul/winfiles
      SYNC_BRANCH: sync/from-dotfiles
    steps:
      - name: Check token and target repo
        id: check
        env:
          GH_TOKEN: ${{ secrets.GH_TOKEN }}
        run: |
          if [ -z "$GH_TOKEN" ]; then
            echo "GH_TOKEN secret not set yet - skipping sync"
            echo "skip=true" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          if ! gh api "repos/$OTHER_REPO" >/dev/null 2>&1; then
            echo "Target repo $OTHER_REPO not reachable yet - skipping sync"
            echo "skip=true" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          echo "skip=false" >> "$GITHUB_OUTPUT"

      - name: Checkout dotfiles
        if: steps.check.outputs.skip != 'true'
        uses: actions/checkout@v4

      - name: Checkout other repo
        if: steps.check.outputs.skip != 'true'
        uses: actions/checkout@v4
        with:
          repository: ${{ env.OTHER_REPO }}
          token: ${{ secrets.GH_TOKEN }}
          path: other

      - name: Copy shared packages into other repo
        if: steps.check.outputs.skip != 'true'
        run: |
          copy_dir() {
            local src="$1" dst="$2"
            [ -d "$src" ] || return 0
            rsync -a --delete "$src/" "other/$dst/"
          }
          # <local path> <path in the other repo>
          copy_dir nvim nvim
          copy_dir starship starship
          copy_dir bat bat
          copy_dir lazygit lazygit
          copy_dir opencode wsl/opencode
          copy_dir zsh wsl/zsh
          copy_dir tmux wsl/tmux
          copy_dir fastfetch fastfetch

      - name: Open or update sync PR
        if: steps.check.outputs.skip != 'true'
        env:
          GH_TOKEN: ${{ secrets.GH_TOKEN }}
        run: |
          cd other
          if [ -z "$(git status --porcelain)" ]; then
            echo "No changes to sync - nothing to do"
            exit 0
          fi
          git config user.name "sync[bot]"
          git config user.email "sync[bot]@users.noreply.github.com"
          git checkout -b "$SYNC_BRANCH"
          git add -A
          git commit -m "chore: sync shared configs from bymaul/dotfiles"
          git push --force origin "$SYNC_BRANCH"
          PR=$(gh pr list --repo "$OTHER_REPO" --head "$SYNC_BRANCH" --state open --json number -q '.[0].number')
          if [ -z "$PR" ]; then
            PR=$(gh pr create --repo "$OTHER_REPO" --base master --head "$SYNC_BRANCH" \
              --title "chore: sync shared configs from bymaul/dotfiles" \
              --body "Auto-synced from bymaul/dotfiles. Merge to apply; resolve conflicts in this PR." \
              --json number -q .number)
          fi
          gh pr merge "$PR" --repo "$OTHER_REPO" --squash --auto || true
