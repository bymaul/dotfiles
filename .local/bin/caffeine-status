#!/bin/bash
# Waybar status for caffeine mode: prints a coffee glyph when active,
# with class "active" so the bar can tint it (see style.css #custom-caffeine).

if pgrep -f -- "--who=Caffeine" > /dev/null; then
    printf '\n\nactive\n'
fi
